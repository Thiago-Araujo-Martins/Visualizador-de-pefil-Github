# Visualizador-de-pefil-Github

Endpoints que vamos usar
perfil: GET /users/:username

repositórios: GET /users/:username/repos ( usar query ´per_page=10 e sort=uptade / sort=pushed conforme objetivo)